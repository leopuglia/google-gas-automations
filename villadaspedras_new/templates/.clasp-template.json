{
  "scriptId": "{{scriptId}}",
  "filePushOrder": [
    {{#each main_files}}
    "{{this}}"{{#unless @last}},{{/unless}}
    {{/each}}
    {{#if utils_files.length}}
    {{#if main_files.length}},{{/if}}
    {{#each utils_files}}
    "{{this}}"{{#unless @last}},{{/unless}}
    {{/each}}
    {{/if}}
    "appsscript.json"
  ]
}
